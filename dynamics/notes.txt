more = np.array(
        [
            z - self.initial_z,
            np.sin(angle_to_target),
            np.cos(angle_to_target),
            0.3 * vx,
            0.3 * vy,
            0.3 * vz,  # 0.3 is just scaling typical speed into -1..+1, no physical sense here
            r,
            p
        ],
        dtype=np.float32)
    return np.clip(np.concatenate([more] + [j] + [self.feet_contact]), -5, +5)


j = [16 numbers] even numbers are joint positions
                 odd numbers are joint velocities

feet_contact = [4 numbers]

total = 28 numbers

we ignore the first three (relative position to target)
